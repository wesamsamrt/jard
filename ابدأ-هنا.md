# تشغيل موقع جرد

الموقع مبني باستخدام HTML وCSS وJavaScript، ومضبوط على مشروع Supabase الذي أرسلته.

## تفعيل الحفظ لأول مرة

1. افتح مشروعك في Supabase، ثم SQL Editor ← New query.
2. انسخ محتوى ملف `setup.sql` المرفق كاملًا وشغّله عبر Run.
3. افتح Authentication ← Users ← Add user وأنشئ مستخدمًا بالبريد وكلمة المرور، مع تأكيد البريد.
4. افتح الموقع واضغط تسجيل الدخول واستخدم ذلك الحساب.
5. اضغط «قالب Excel» للحصول على الأعمدة الصحيحة، ثم «جرد جديد» وارفع ملفك.

## الاستخدام

- ترتيب الأعمدة: اسم الصنف، الموديل (اختياري)، كود الصنف، كمية الجرد الفعلي1، كمية الجرد الفعلي2.
- ضع أكواد الأصناف في Excel بصيغة نص، خاصة الأكواد الطويلة أو التي تبدأ بصفر. لا يمكن استرجاع أرقام حذفها Excel بالفعل.
- يُستورد كل صف مستقلًا، حتى عند تكرار كود الصنف.
- اترك الكمية فارغة إذا لم يُنفذ الجرد. الصفر يعني كمية فعلية صفر.
- ابحث بالاسم أو الكود. إذا طابق البحث صنفًا واحدًا، اضغط Enter لفتح إدخال الكمية.
- «حفظ الكمية» يحفظ الكميتين في Supabase؛ عند فشل الاتصال تبقى المدخلات في النافذة للمحاولة مجددًا.
- «سجل الجرد» يفتح جلسات حسابك السابقة. استخدم الحساب نفسه على أجهزتك الأخرى.
- «تصدير Excel» يصدر كامل الجلسة بالأعمدة الخمسة، حتى عند وجود بحث أو تصفية.
- كل مستخدم يرى جلساته فقط؛ لا توجد مشاركة جلسات بين مستخدمين مختلفين.
- النموذج التجريبي مؤقت ولا يُحفظ في قاعدة البيانات.

## الملفات

- index.html: الواجهة العربية.
- style.css: التصميم المتجاوب.
- app.js: الاستيراد والبحث والجرد والتصدير والمصادقة.
- config.js: رابط المشروع والمفتاح العام. لا تضع مفتاح service_role هنا.
- setup.sql: الجداول والصلاحيات ودالة الاستيراد الذري.

يمكن استضافة هذه الملفات على أي استضافة ملفات ثابتة تدعم HTTPS. تحتاج الصفحة إلى الإنترنت لتحميل مكتبات Excel وSupabase والخط.

## التحقق

تم اختبار قراءة وكتابة Excel، وبقاء الأصفار في أكواد النص، والتفرقة بين الصفر والفراغ، والكميات العربية، ورفض المدخلات غير الصالحة، والبحث وتصدير كامل الجلسة. تحقق الاتصال بمشروع Supabase وأظهر أن الجداول لم تُنشأ وقت الإعداد؛ الاختبار الكامل للحفظ وإعادة الفتح يحتاج تنفيذ setup.sql وتسجيل الدخول.

## إضافة الموديل إلى قاعدة موجودة
شغّل upgrade-model.sql مرة واحدة في SQL Editor. الموديل اختياري ويأتي بعد اسم الصنف. تُقبل الملفات القديمة دون هذا العمود، ويمكن تعديله من نافذة إدخال الكمية.

يُعد الصنف مكتمل الجرد عند إدخال كمية الجرد الفعلي 1، بما فيها الصفر. كمية الجرد الفعلي 2 اختيارية، ولا تُحسب الفروق إلا إذا أُدخلت الكميتان.
